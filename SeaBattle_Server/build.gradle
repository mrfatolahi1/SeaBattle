plugins {
    id 'java'
}

group 'org.example'
version '1.0.json-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.7.0.json'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.7.0.json'

    // https://mvnrepository.com/artifact/com.fasterxml.jackson.core/jackson-databind
    implementation group: 'com.fasterxml.jackson.core', name: 'jackson-databind', version: '2.12.3'

    // https://mvnrepository.com/artifact/com.fasterxml.jackson.core/jackson-core
    implementation group: 'com.fasterxml.jackson.core', name: 'jackson-core', version: '2.12.3'

    implementation files('../SeaBattle_Events/build/classes/java/main')
}

test {
    useJUnitPlatform()
}